apiVersion: apps/v1
kind: Deployment
metadata:
  name: releases
  labels:
    app: releases
spec:
  replicas: 2
  selector:
    matchLabels:
      app: releases
  template:
    metadata:
      labels:
        app: releases
    spec:
      serviceAccountName: releases
      containers:
      - name: releases
        image: crowdunlocked/releases:latest
        ports:
        - containerPort: 8080
        env:
        - name: PORT
          value: "8080"
        - name: DYNAMODB_TABLE
          value: releases
        - name: AWS_REGION
          value: us-east-1
        - name: AWS_XRAY_DAEMON_ADDRESS
          value: xray-service.default:2000
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: releases
spec:
  selector:
    app: releases
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: releases
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/releases-service-role
